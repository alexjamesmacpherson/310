<% provide(:title, "Users") %>
<h1>All <%= current_user.school_id == 1 ? "Super Users" : "Users at #{school_name}" %></h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>
